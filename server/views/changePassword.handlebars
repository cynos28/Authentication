<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        background-color: aliceblue;
        padding: 30px;
        font-family: Arial, Helvetica, sans-serif;
      }
      .container {
        background-color: #ffffff;
        padding: 10px;
        border-radius: 3px;
        border-radius: 1rem;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
      }

      .color-primary {
        color: teal;
      }
      .color-danger {
        color: orangered;
      }
      .color-success {
        color: #28a745;
      }
      .color-white {
        color: #fff;
      }
      .color-blue {
        color: #0a1930;
      }

      a {
        font-size: 1.4rem;
        text-decoration: none;
      }

      .btn {
        font-size: 1.4rem;
        font-weight: 400;
        padding: 6px 8px;
        margin: 0 5px 0 0;
        border: 1px solid transparent;
        border-radius: 3px;
        cursor: pointer;
      }

      .btn-primary {
        color: #fff;
        background: teal;
      }
      .btn-secondary {
        color: #fff;
        border: 1px solid #fff;
        background: transparent;
      }
      .btn-danger {
        color: #fff;
        background: orangered;
      }
      .btn-success {
        color: #fff;
        background: #28a745;
      }

      .flex-center {
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .logo {
        padding: 5px;
        background-color: teal;
        border-radius :1rem
      }

      .my {
        margin: 2rem 0;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="logo flex-center ">
        <h2 class="color-white">PrimeLodge</h2>
      </div>
      <h2>Hello <span class="color-danger">{{name}}</span></h2>
      <p>Your password change successfully. </p>
      <p>If this is not ypu, please reset your password Immediately.</p>

      <a href={{link}} class="btn btn-success">Reset Password</a>

      <div class="my">
        <p>Regards...</p>
        <p><b class="color-danger">PrimeLodge</b> </p>
      </div>
    </div>
  </body>
</html>
